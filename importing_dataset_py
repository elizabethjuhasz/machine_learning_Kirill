dataset = pd.read_csv('Data.csv')
X = dataset.iloc[:, :-1].values
# line two means that you include all the columns except the last
# matrix for IVs

Y = dataset.iloc[:, 3].values
# include last column
# remember indices start at 0
# remember to always set wd

# override restrction for printing too many lines run the following
# Run 
np.set_printoptions(threshold = np.nan) 
# or 
np.set_printoptions(threshold=np.inf) 

